'use client';

import TeamManager from '@/features/iam/components/TeamManager';

export default function IAMSettingsPage() {
  return (
    <div className="space-y-10 animate-in fade-in duration-700">
      <TeamManager />
    </div>
  );
}

